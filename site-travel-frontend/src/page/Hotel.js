import React from "react";
import SearchBar from "../components/SearchBar";

const Hotel = () => {
  return (
    <div>
      <SearchBar travelType="hotels" />
    </div>
  );
};

export default Hotel;
