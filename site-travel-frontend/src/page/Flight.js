import React from "react";
import SearchBar from "../components/SearchBar";

const Flight = () => {
  return (
    <div>
      <SearchBar travelType="flights" />
    </div>
  );
};

export default Flight;
