import React from "react";
import SearchBar from "../components/SearchBar";

const Activity = () => {
  return (
    <div>
      <SearchBar travelType="activities" />
    </div>
  );
};

export default Activity;
